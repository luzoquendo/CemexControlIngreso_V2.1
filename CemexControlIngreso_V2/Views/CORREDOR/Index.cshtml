@model IEnumerable<CemexControlIngreso_V2.Models.CORREDOR>

@{
    ViewBag.Title = "Corredor Vial";
}

<h2>Corredor Vial</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Corredor1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Corredor1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdCorredor }) |
            @Html.ActionLink("Consultar", "Details", new { id=item.IdCorredor }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.IdCorredor })
        </td>
    </tr>
}

</table>
