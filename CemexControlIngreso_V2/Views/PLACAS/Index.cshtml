@model IEnumerable<CemexControlIngreso_V2.Models.PLACAS>

@{
    ViewBag.Title = "Placas";
}

<h2>Placas</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Placa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.INSTRUCTOR.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Placa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.INSTRUCTOR.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.IdPlaca }) |
            @Html.ActionLink("Consultar", "Details", new { id = item.IdPlaca }) |
            @Html.ActionLink("Borrar", "Delete", new { id = item.IdPlaca })
        </td>
    </tr>
}

</table>
